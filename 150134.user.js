// ==UserScript==
// @name        A-P search manga as default
// @namespace   http://localhost
// @include     http://www.anime-planet.com/*
// @version     1
// ==/UserScript==

var selectContainer = document.getElementById('selectContainer');
if  (selectContainer != null)
{
	var selectSource = document.getElementById('selectSource');
	selectSource.children[0].selected = "";
	selectSource.children[1].selected = "selected";
	// The rest doesn't work as the selectTarget is generated by pages own script
	/*var selectTarget = selectContainer.children[1];//document.getElementById('selectTarget');
	selectTarget.children[0].innerHTML = selectTarget.children[1].children[0].children[1].innerHTML;*/
}