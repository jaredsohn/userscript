// ==UserScript==
// @name	Auto Refresh
// @namespace	BARRET
// @homepage    BARRET
// @description http://www.streetms.com*
// @include	http://ask.com*

// ==/UserScript==
var dtime, sites, timeS;
sites = new Array();
dtime = new Array();
timeS = new Array();

//////////////////////////////////////////////////////////////////////////////
////////////////////////// SET HERE //////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////
//////// Create more sites following the pattern if needed ///////////////////
//////// For the timeS, enter minutes, hours, or seconds /////////////////////
//////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////
sites[0] = 'http://www.streetms.com'; // site one
dtime[0] = 9; // time for site one
timeS[0] = 'seconds'; // time setting for site one

sites[1] = 'google.ca'; // site two
dtime[1] = 9; // time for site two
timeS[1] = 'seconds'; // time setting for site two

sites[2] = 'yahoo.com'; // site three
dtime[2] = 9; // time for site three
timeS[2] = 'seconds'; // time setting for site three








function reload(site,theTime,timeSS)
{
var time = 9999*9999*9999;
if(timeSS == 'minutes') {time = theTime * 60 * 1000;}
if(timeSS == 'hours') {time = theTime * 1000 * 60 * 60;}
if(timeSS == 'seconds') {time = theTime * 1000;}
window.setTimeout("document.location.reload();", time);
}

for(var i=0; i<sites.length; i++)
{
if(location.href.indexOf(sites[i]) != -1)
{
reload(sites[i], dtime[i], timeS[i]);
}
}