// ==UserScript==
// @name       My Fancy New Userscript
// @namespace  http://use.i.E.your.homepage/
// @version    0.1
// @description  enter something useful
// @match      http://vk.com/nyako_desu
// @copyright  2012+, You
// ==/UserScript==
window.onload=function(){
all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Дмитрий Шульга/g, 'Kliera');
            
all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Николай Егоров/g, 'Higawa_');
            

all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Александр Кондренко/g, 'Crash51');
            
            
all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Костян Вертяшкин/g, 'post_modern');
            
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Александр Барышев/g, 'Colt94');
            
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Александр Конашенко/g, 'AKlTO');
            
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Кирилл Сергеев/g, 'Mr_Nexsis');
            
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/User Ex/g, 'foier');
           
           
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Женя Иванников/g, 'Maulder2010');
            
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Сергей Пуляев/g, 'expo3');
            
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Михаил Шевченко/g, 'ReapeRday');
            
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Тень Студента/g, 'nekorais');
            
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Павел Кирьянов/g, 'Bullets_for_everyone');
            
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Илья Рыбаков/g, 'Jhnx');
            
           
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Антон Добранов/g, 'Pro_AfoHbKa');
            
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Кирилл Кульбако/g, 'x26nexus');
            
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Жека Трегубенко/g, 'Iegion43');
            
           
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Катерина Метасёва/g, 'MIYOSHI');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Денис Мельник/g, 'DemianDahok');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Антон Ноцтис/g, 'Gunblade9000');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Рихард Кельм/g, 'Rikhard1');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Аманкелды Толеубеков/g, 'Takuto777');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Николай Бутусов/g, 'xNikzzz');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Радик Кагиров/g, '_ScorpEX_');
           
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Даниил Грицык/g, 'danik');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Василий Шарапов/g, 'I_Vasya_I');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Денис Карагулько/g, 'lfr13');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Александр Свитнев/g, 'AkumaKitsune');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Константин Иванников/g, 'satir66');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Денис Мельник/g, 'DemianDahok');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Денис Сафронов/g, 'Ton4a');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Глеб Chan/g, 'bascvosher');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Дохлый Лось/g, 'Manha_ttan');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Ааа Ббб/g, 'post_me');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Филипп Мицук/g, 'Tido');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Михаил Гудилин/g, 'domishka');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Антон Дварф/g, 'dworff5');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Konstantin Orlov/g, 'PaxDeorum');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Artem Incognito/g, 'krash73');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Дима Алешин/g, 'Flashback1995');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Николай Кулишенко/g, 'Great_Teacher');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Роман Семёнов/g, 'Sonic50');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Макар Андреев/g, 'slop_666');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Даниил Держо/g, 'Jlero4ka');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Сергей Боталов/g, 'EVANGELION59');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Игорь Горбатюк/g, 'Q_ir');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Dziro ^_^/g, 'D_ziro');
            
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Александр Черников/g, 'lRamsl');
            
all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Никита Куцкин/g, 'LLIKoloLoLLIa_OloLoLLIa');
            
            all_elements=document.getElementsByTagName('*');
for(i=0,s=all_elements.length;i<s;i++)
	if(all_elements[i].tagName!="SCRIPT")
		for(j=0,s_=all_elements[i].childNodes.length;j<s_;j++)
			if(all_elements[i].childNodes[j].nodeType==3)
                all_elements[i].childNodes[j].textContent=all_elements[i].childNodes[j].textContent.replace(/Лёха Тихонов/g, 'AHiMeLLlHiK_B_TAHKE');
}
            