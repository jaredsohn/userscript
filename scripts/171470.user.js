// ==UserScript==
// @name         Leo 
// @namespace    http://use.i.E.your.homepage/
// @version      0.1
// @description  my userscript
// @match        http://*/*
// @require      http://code.jquery.com/jquery-1.9.0.min.js
// ==/UserScript==

/* 
 * // ==UserScript==   	这个注释块很重要，而且要严格匹配（成对出现）油猴使用它们来标记metadata的开始和结束，这个注释块可以放在在脚本的任何地方，但是通常放在靠近开头的位置。
 * // @name		这是插件的名称,@name 是可选的,也就是可以不用为插件起名字.如果存在,它可能只出现一次.如果不存在,它将默认为用户的脚本文件名减去".user.js"
 * // @namespace	这一项是网址（URL），油猴使用它来区分拥有相同名称但是由不同的开发人员书写的插件，如果你有域名（或者二级域名的等），你可以用它作为namespace，或者你可以使用tag: URI (此链接已坏).@namespace 也是可选项，如果存在，它可能只出现一次。如果不存在，它将默认为用户的下载该脚本文件的网址。
 * // @version		版本
 * // @description	描述，介绍
 * // @match		在哪个网站上运行这个脚本，* 作为域的名称或路径的一部分的简单通配符字符
 * // @resource		得到的是资源
 * // @require		一般用来调用库的，它会在脚本运行之前先执行。比如调用jQuery
 * // ==/UserScript==	
 */