// ==UserScript==
// @name           bungmonkey/get to the choppa backgrounds
// @namespace      bungmonkey/get to the choppa backgrounds
// @description    Changes the background depending on the time of day.
// @include        http://*bungie.net/*
// @exclude        http://*bungie.net/Projects/ODST/*
// ==/UserScript==

var d = new Date();
var time = d.getHours();

if (time < 14)

{

function addGlobalStyle(css) {
    var head, style;
    head = document.getElementsByTagName('head')[0];
    if (!head) { return; }
    style = document.createElement('style');
    style.type = 'text/css';
    style.innerHTML = css;
    head.appendChild(style);
}

addGlobalStyle(http://i565.photobucket.com/albums/ss92/XBLmonster/Bungie_Logo_Noon.png);

}

if (time < 14)

{

function addGlobalStyle(css) {
    var head, style;
    head = document.getElementsByTagName('head')[0];
    if (!head) { return; }
    style = document.createElement('style');
    style.type = 'text/css';
    style.innerHTML = css;
    head.appendChild(style);
}

addGlobalStyle(http://i565.photobucket.com/albums/ss92/XBLmonster/test.gif);

}

else {

function addGlobalStyle(css) {
    var head, style;
    head = document.getElementsByTagName('head')[0];
    if (!head) { return; }
    style = document.createElement('style');
    style.type = 'text/css';
    style.innerHTML = css;
    head.appendChild(style);
}

addGlobalStyle(// ==UserScript==
// @name           Bungie.net Time-Changing Background
// @namespace      Bungie.net Time-Changing Background
// @description    Changes the background depending on the time of day.
// @include        http://*bungie.net/*
// @exclude        http://*bungie.net/Projects/ODST/*
// ==/UserScript==

var d = new Date();
var time = d.getHours();

if (time < 14)

{

function addGlobalStyle(css) {
    var head, style;
    head = document.getElementsByTagName('head')[0];
    if (!head) { return; }
    style = document.createElement('style');
    style.type = 'text/css';
    style.innerHTML = css;
    head.appendChild(style);
}

addGlobalStyle(http://i565.photobucket.com/albums/ss92/XBLmonster/test.gif);

}

if (time < 14)

{

function addGlobalStyle(css) {
    var head, style;
    head = document.getElementsByTagName('head')[0];
    if (!head) { return; }
    style = document.createElement('style');
    style.type = 'text/css';
    style.innerHTML = css;
    head.appendChild(style);
}

addGlobalStyle(http://i565.photobucket.com/albums/ss92/XBLmonster/test.gif);

}

else {

function addGlobalStyle(css) {
    var head, style;
    head = document.getElementsByTagName('head')[0];
    if (!head) { return; }
    style = document.createElement('style');
    style.type = 'text/css';
    style.innerHTML = css;
    head.appendChild(style);
}

addGlobalStyle(http://i565.photobucket.com/albums/ss92/XBLmonster/Bungie_Logo_Noon.png);

}

// me so BUNG MONKEY!!!);

}

// me so sexi