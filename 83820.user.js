// ==UserScript==// @name           Remove Google homepage fade-in// @description    In its most recent revamp of its homepage, Google implemented a "fade in" of most of its page elements that doesn't occur until the user moves the mouse.  This script forces the fade-in at page load.// @include        http://www.google.com/// @require        http://ajax.googleapis.com/ajax/libs/jquery/1.3.2/jquery.min.js// ==/UserScript==$(document).ready(function() {   $('#ghead, #sbl, #fctr, #tba, #tbe, #pmocntr, #cpFooter, #cpNavContainer, #cpNavTextWrapper, #cpNavLink').css('opacity','1').show();   document.addEventListener('mousemove',function(e){e.stopPropagation();}, true);   document.forms.namedItem('f').elements.namedItem('q').setAttribute('onblur', '');});