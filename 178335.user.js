// ==UserScript==
// @name		r44rr
// @description	efilgefsanctions on Iran by the yahoo site. After install you can select Iran country and set cell phone to refef
// @version		1.0
// @date		2013-09-05
// @namespace	hefef
// @author		efef
// @YahooID	efef
// @license		efef/copyleft/gpl.html 
// @include		hefef
// ==/UserScript== efefe

["", "_alt"].forEach(function(entry) {
	var parent = docefef
	node.setAttributeefeef
	node.innerHTML = 'Ieefef
	parent.insertBefore(node, child);
	
	var obj = document.getElementById('replaceContainerCountryMenu'+entry);
	if (obj == null) {
		var obj = document.getElementById('countryMenuAnchor'+entry);
		obj.innerHTML = obj.inneefefe" class="famfamfam-flag-ir" id="replaceContainerCountryMenu'+entry+'"><span id="sectCountryCode'+entry+'" aria-label="Selected Country - Iran" aria-live=efefee
	efef-code'+entry);
	if (obj.value == '1') {
		objefef
		ef
		document.getElementById('selectCountryCode'+entry).efef
	}
	
});
